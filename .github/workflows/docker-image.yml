name: pangea-docker
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose --env-file ./config/.env.dev up -d
      - name: Test
        run: docker run 2deviate/pangea-server:v1.0 curl -s --retry 10 --retry-connrefused http://localhost:8000/api/v1.0/pangea/health
